{
  "$schema": "../../../../../schema/src/json-schemas/repository.schema.json",
  "name": "temporary",
  "description": "用于存储临时数据",
  "scope": "container",
  "parameters": {
    "defaultValue": {
      "label": "默认值",
      "type": "json",
      "defaultValue": {},
      "description": "默认值仅支持 JSON 格式编辑"
    }
  },
  "actions": {
    "setValue": {
      "description": "为临时存储设置值",
      "enabledPath": true,
      "enabledValue": true
    }
  }
}
