{
  "$schema": "../../../schema/src/json-schemas/widget.schema.json",
  "tag": "stack",
  "properties": {
    "direction": {
      "label": "方向",
      "schema": {
        "enum": ["column-reverse", "column", "row-reverse", "row"]
      },
      "defaultValue": "row"
    },
    "divider": {
      "label": "是否有分割线",
      "schema": {
        "type": "boolean"
      },
      "defaultValue": false
    },
    "spacing": {
      "label": "留白",
      "schema": {
        "type": "integer"
      },
      "defaultValue": 0
    }
  }
}
