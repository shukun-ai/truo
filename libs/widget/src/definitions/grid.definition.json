{
  "$schema": "../../../schema/src/json-schemas/widget.schema.json",
  "tag": "grid",
  "properties": {
    "columns": {
      "label": "列数",
      "schema": {
        "type": "integer"
      },
      "defaultValue": 3
    },
    "horizontalSpacing": {
      "label": "水平留白",
      "schema": {
        "enum": ["xs", "sm", "md", "lg", "xl"]
      }
    },
    "verticalSpacing": {
      "label": "垂直留白",
      "schema": {
        "enum": ["xs", "sm", "md", "lg", "xl"]
      }
    }
  }
}
