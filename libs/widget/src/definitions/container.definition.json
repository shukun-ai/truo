{
  "$schema": "../../../schema/src/json-schemas/widget.schema.json",
  "tag": "container",
  "properties": {
    "maxWidth": {
      "label": "最大宽度",
      "schema": {
        "enum": ["xs", "sm", "md", "lg", "xl"]
      },
      "defaultValue": "md"
    },
    "fixed": {
      "label": "固定宽度",
      "schema": {
        "type": "boolean"
      }
    },
    "disableGutters": {
      "label": "两端留白",
      "schema": {
        "type": "boolean"
      }
    }
  }
}
