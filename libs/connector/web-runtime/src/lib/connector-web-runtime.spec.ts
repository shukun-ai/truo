import { connectorWebRuntime } from './connector-web-runtime';

describe('connectorWebRuntime', () => {
  it('should work', () => {
    expect(connectorWebRuntime()).toEqual('connector-web-runtime');
  });
});
