import {
  IconCopy,
  IconDots,
  IconInfoCircle,
  IconPlus,
  IconSignature,
  IconTrash,
} from '@tabler/icons-react';

export const iconsMap = {
  trash: IconTrash,
  rename: IconSignature,
  copy: IconCopy,
  plus: IconPlus,
  more: IconDots,
  info: IconInfoCircle,
} as const;
