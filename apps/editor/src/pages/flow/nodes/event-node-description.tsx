import { FlowEvent } from '@shukun/schema';
import React, { FunctionComponent } from 'react';

export interface EventNodeDescriptionProps {
  event: FlowEvent;
}

export const EventNodeDescription: FunctionComponent<
  EventNodeDescriptionProps
> = () => {
  return null;
};
