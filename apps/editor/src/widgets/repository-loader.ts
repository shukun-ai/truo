import { editorRepositories } from '@shukun/presenter/definition';
import { RepositorySchema } from '@shukun/schema';

export const repositoryDefinitions: Record<string, RepositorySchema> =
  editorRepositories;
