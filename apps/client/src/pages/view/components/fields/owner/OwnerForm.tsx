import { LegacyFunctionComponent } from '@shukun/component';
import React, { FunctionComponent } from 'react';

import { InputFieldProps } from '../interfaces';

export const OwnerForm: LegacyFunctionComponent<InputFieldProps> = () => {
  return <>暂不支持所有者</>;
};
