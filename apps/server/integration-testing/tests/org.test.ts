describe('org', () => {
  describe('GET public/:orgName/org', () => {
    it('When request a org api, then return basic information without secret information.', () => {
      expect(1).toEqual(1);
    });
  });
});
